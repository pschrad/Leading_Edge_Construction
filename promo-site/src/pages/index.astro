---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import sharedBackground from '../assets/new-turf-installation.jpg';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body class="home-page">
		<Header />
		<main class="home">
			<section class="hero">
				<div class="hero__overlay">
					<div class="hero__content wrap">
					<p class="hero__eyebrow">Annapolis &amp; Chesapeake Bay outdoor pros</p>
					<h1>Custom turf, pools, and backyard escapes built to last</h1>
					<p class="hero__subtitle">
						For 20 years our background-checked crew has delivered dream lawns, water features, and outdoor
						rooms for Maryland homes and businesses—handling every size project from cozy courtyards to
						multi-acre retreats.
					</p>
						<div class="hero__buttons">
							
							<a class="button button--outline" href="/blog">Call us</a>
						</div>
					</div>
				</div>
			</section>

			<section class="credibility">
				<div class="wrap">
					<p class="eyebrow">Trusted by neighbors across Anne Arundel &amp; the Chesapeake Bay</p>
					<ul class="credibility__list">
						<li>Annapolis</li>
						<li>Davidsonville</li>
						<li>Severna Park</li>
						<li>Arnold</li>
						<li>Edgewater</li>
					</ul>
				</div>
			</section>

			<section class="services" id="services">
				<div class="wrap">
					<div class="section-header">
						<p class="eyebrow">Signature services</p>
						<h2>Everything you need to shape your private oasis</h2>
						<p>
							We tailor each project to your property, lifestyle, and timeline—combining imaginative
								design with construction precision and concierge-level communication.
						</p>
					</div>
					<div class="services__grid">
						<article class="service-card">
							<h3>Custom pools & spas</h3>
							<p>
								Vanishing edges, baja shelves, glass tile spas, and statement lighting curated to match your
								architecture.
							</p>
							<a class="link-arrow" href="/services/pools-and-spas">Explore pools & spas</a>
						</article>
						<article class="service-card">
							<h3>Outdoor living environments</h3>
							<p>
								Covered lounges, summer kitchens, fire features, and immersive audio-visual setups built for
								year-round entertaining.
							</p>
							<a class="link-arrow" href="/services">See outdoor living</a>
						</article>
						<article class="service-card">
							<h3>Full-site enhancements</h3>
							<p>
								Drainage, retaining, pavers, and landscape lighting that ensure longevity and effortless
								maintenance.
							</p>
							<a class="link-arrow" href="/services">View site services</a>
						</article>
					</div>
				</div>
			</section>

		

			<section class="process">
				<div class="wrap">
					<p class="eyebrow">Our approach</p>
					<h2>Proven process, concierge experience</h2>
					<ol class="process__steps">
						<li>
							<h3>Vision & discovery</h3>
							<p>
								We collaborate on-programming, inspiration, and budget alignment to build a shared vision of
								your outdoor lifestyle.
							</p>
						</li>
						<li>
							<h3>Design development</h3>
							<p>
								Concept boards evolve into technical plans, engineering, and permit-ready documentation,
								supported by immersive visuals.
							</p>
						</li>
						<li>
							<h3>Build & refinement</h3>
							<p>
								Your dedicated construction lead coordinates trades, quality checks, and weekly walkthroughs
								until the final reveal.
							</p>
						</li>
					</ol>
				</div>
			</section>

			<section class="testimonials">
				<div class="wrap">
					<p class="eyebrow">Client reflections</p>
					<h2>"Every detail exceeded our expectations"</h2>
					<div class="testimonials__grid">
						<blockquote>
							<p>
								"From the first design meeting through installation, the team anticipated every decision. Our
									backyard is now the centerpiece of family gatherings."
							</p>
							<footer>— The Ramirez family, Winter Park</footer>
						</blockquote>
						<blockquote>
							<p>
								"Weekly progress updates and immaculate craftsmanship. They transformed a blank canvas into a
									resort-level experience."
							</p>
							<footer>— Jason & Claire, Lake Mary</footer>
						</blockquote>
					</div>
				</div>
			</section>

			
		</main>
		<Footer />
		<style define:vars={{ sharedImage: `url(${sharedBackground.src})` }}>
			:global(body.home-page) {
				background-image: linear-gradient(rgba(4, 14, 20, 0.45), rgba(4, 14, 20, 0.68)), var(--sharedImage);
				background-position: center;
				background-size: cover;
				background-attachment: fixed;
				margin: 0;
				color: #ffffff;
			}
			.hero {
				position: relative;
				min-height: clamp(28rem, 90vh, 44rem);
				display: grid;
				align-items: center;
				color: #ffffff;
				background-image: var(--sharedImage);
				background-size: cover;
				background-position: center;
				background-repeat: no-repeat;
			}
			.home {
				max-width: none;
				width: 100%;
				padding: 0;
				color: #0b0907;
			}
			.hero::after {
				content: '';
				position: absolute;
				top: 0;
				right: 0;
				bottom: 0;
				left: 0;
				background: linear-gradient(180deg, rgba(5, 25, 32, 0.4) 0%, rgba(7, 24, 31, 0.5) 55%, rgba(12, 32, 38, 0.6) 100%);
			}
			.hero__overlay {
				position: relative;
				z-index: 1;
				display: flex;
				flex-direction: column;
				gap: clamp(3.5rem, 9vh, 5.5rem);
				padding: clamp(4rem, 12vh, 6rem) 0 clamp(3rem, 10vh, 5rem);
			}
			.hero__content {
				display: grid;
				justify-items: center;
				text-align: center;
				gap: 1.5rem;
				max-width: 840px;
			}
			.hero__eyebrow {
				text-transform: uppercase;
				letter-spacing: 0.18em;
				font-size: 0.8rem;
				font-weight: 600;
				color: rgba(255, 255, 255, 0.7);
			}
			.hero h1 {
				margin: 0;
				font-size: clamp(2.4rem, 6vw, 3.8rem);
				line-height: 1.05;
			}
			.hero__subtitle {
				margin: 0;
				font-size: clamp(1rem, 2.8vw, 1.2rem);
				color: rgba(255, 255, 255, 0.76);
			}
			.hero__buttons {
				display: flex;
				flex-wrap: wrap;
				gap: 1rem;
				justify-content: center;
			}
			.button {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				gap: 0.5rem;
				padding: 0.85rem 1.9rem;
				border-radius: 999px;
				border: 2px solid transparent;
				font-size: 0.95rem;
				font-weight: 600;
				text-decoration: none;
				transition: transform 120ms ease, box-shadow 120ms ease, background 120ms ease, border-color 120ms ease;
			}
			.button--primary {
				background: rgba(31, 149, 204, 0.92);
				color: #ffffff;
				box-shadow: 0 16px 34px rgba(17, 86, 118, 0.35);
			}
			.button--primary:hover {
				transform: translateY(-2px);
				box-shadow: 0 20px 38px rgba(17, 86, 118, 0.4);
			}
			.button--outline {
				border-color: rgba(255, 255, 255, 0.75);
				color: #ffffff;
				background: transparent;
			}
			.button--outline:hover {
				border-color: #ffffff;
				background: rgba(255, 255, 255, 0.12);
			}
			.button--inverse {
				background: #fdf9f4;
				color: #0f6a6a;
				border-color: rgba(253, 249, 244, 0.4);
			}
			.button--inverse:hover {
				transform: translateY(-1px);
			}
			.credibility {
				padding: 2.5rem 0;
				background: #f1e5d4;
				border-block: 1px solid rgba(160, 111, 42, 0.18);
			}
			.wrap {
				width: min(1120px, 92vw);
				margin: 0 auto;
			}
			.eyebrow {
				text-transform: uppercase;
				letter-spacing: 0.14em;
				font-size: 0.75rem;
				font-weight: 600;
				color: #a06f2a;
				margin-bottom: 1rem;
			}
			.credibility__list {
				list-style: none;
				margin: 0;
				padding: 0;
				display: flex;
				flex-wrap: wrap;
				gap: 1.5rem 2.5rem;
				font-weight: 600;
				color: #5b4b36;
			}
			.section-header {
				max-width: 42rem;
			}
			.section-header h2 {
				font-size: clamp(1.8rem, 3vw, 2.6rem);
				margin-bottom: 1rem;
			}
			.section-header p {
				color: #5b4b36;
			}
			.services {
				padding: clamp(4rem, 6vw, 6rem) 0;
			}
			.services__grid {
				display: grid;
				gap: 1.8rem;
				grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
				margin-top: clamp(2rem, 4vw, 3rem);
			}
			.service-card {
				background: #ffffff;
				border-radius: 20px;
				padding: 2rem;
				box-shadow: 0 14px 36px rgba(69, 55, 37, 0.1);
				display: grid;
				gap: 1rem;
			}
			.service-card p {
				color: #5f554d;
				margin: 0;
			}
			.link-arrow {
				text-decoration: none;
				font-weight: 600;
				color: #0f6a6a;
				display: inline-flex;
				align-items: center;
				gap: 0.35rem;
			}
			.link-arrow::after {
				content: '→';
				transition: transform 120ms ease;
			}
			.link-arrow:hover::after {
				transform: translateX(3px);
			}
			.highlight {
				background-image: linear-gradient(120deg, rgba(15, 106, 106, 0.85), rgba(8, 56, 62, 0.75)), var(--sharedImage);
				background-size: cover;
				background-position: center;
				color: #fdf9f4;
				padding: clamp(4rem, 7vw, 6rem) 0;
			}
			.highlight__inner {
				display: grid;
				gap: clamp(2rem, 4vw, 4rem);
				align-items: center;
			}
			.highlight__content h2 {
				font-size: clamp(2rem, 3.5vw, 2.8rem);
				margin-bottom: 1rem;
			}
			.highlight__content p {
				color: rgba(253, 249, 244, 0.88);
				margin-bottom: 2rem;
			}
			.highlight__stats {
				list-style: none;
				padding: 0;
				margin: 0 0 2rem;
				display: grid;
				gap: 0.8rem;
			}
			.highlight__stats span {
				font-weight: 700;
				font-size: 1.05rem;
			}
			.highlight__media {
				width: 100%;
				min-height: 320px;
				border-radius: 24px;
				box-shadow: 0 18px 42px rgba(11, 51, 51, 0.4);
				background-image: var(--sharedImage);
				background-size: cover;
				background-position: center;
			}
			.process {
				padding: clamp(4rem, 6vw, 6rem) 0;
				background: #fdf9f4;
			}
			.process__steps {
				counter-reset: step;
				list-style: none;
				padding: 0;
				margin: clamp(2rem, 3vw, 3rem) 0 0;
				display: grid;
				gap: clamp(1.5rem, 3vw, 2.5rem);
				grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
			}
			.process__steps li {
				background: #ffffff;
				border-radius: 20px;
				padding: 2rem;
				box-shadow: 0 12px 30px rgba(69, 55, 37, 0.08);
				position: relative;
			}
			.process__steps li::before {
				counter-increment: step;
				content: counter(step);
				position: absolute;
				top: -18px;
				left: 24px;
				width: 36px;
				height: 36px;
				display: grid;
				place-items: center;
				background: #0f6a6a;
				color: #fdf9f4;
				border-radius: 50%;
				font-weight: 700;
			}
			.process__steps h3 {
				margin-top: 0.5rem;
				margin-bottom: 0.8rem;
			}
			.process__steps p {
				color: #5f554d;
			}
			.testimonials {
				padding: clamp(4rem, 6vw, 6rem) 0;
			}
			.testimonials h2 {
				font-size: clamp(1.8rem, 3vw, 2.4rem);
				margin-bottom: 2rem;
				max-width: 32rem;
			}
			.testimonials__grid {
				display: grid;
				gap: clamp(1.5rem, 3vw, 2.5rem);
				grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
			}
			.testimonials blockquote {
				background: #ffffff;
				border-radius: 20px;
				padding: 2.2rem;
				box-shadow: 0 14px 36px rgba(69, 55, 37, 0.08);
				margin: 0;
				color: #4f463f;
				font-style: normal;
			}
			.testimonials blockquote footer {
				margin-top: 1.5rem;
				font-weight: 600;
				color: #2f2a25;
			}
			.cta {
				padding: clamp(4rem, 7vw, 6.5rem) 0 clamp(5rem, 8vw, 7rem);
				background: radial-gradient(circle at top, rgba(15, 106, 106, 0.15), transparent 55%),
					linear-gradient(120deg, #2f534d 0%, #0f6a6a 50%, #0e4d54 100%);
				color: #fdf9f4;
			}
			.cta__inner {
				display: flex;
				flex-direction: column;
				gap: 2rem;
				align-items: flex-start;
			}
			.cta__inner h2 {
				font-size: clamp(2rem, 3.5vw, 2.8rem);
				margin-bottom: 1rem;
			}
			.cta__inner p {
				margin: 0;
				max-width: 28rem;
				color: rgba(253, 249, 244, 0.85);
			}
			@media (min-width: 768px) {
				.cta__inner {
					flex-direction: row;
					align-items: center;
					justify-content: space-between;
				}
			}
			@media (max-width: 960px) {
				
			}
			@media (max-width: 640px) {
				.wrap {
					width: min(1120px, 88vw);
				}
				.hero__overlay {
					gap: 3.5rem;
					padding: 5rem 0 4.25rem;
				}
				.hero__content {
					gap: 2.25rem;
				}
				.hero h1 {
					font-size: 2.3rem;
					line-height: 1.1;
				}
				.hero__subtitle {
					font-size: 1.05rem;
					line-height: 1.55;
				}
				.cta {
					padding: 4.5rem 0 6rem;
				}
				.cta__inner {
					gap: 2.5rem;
				}
				.hero__buttons {
					margin-top: 0.25rem;
				}
			}
		</style>
	</body>
</html>
